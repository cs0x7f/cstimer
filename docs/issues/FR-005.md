# [FR-005] Skill Stats and Session Review

## Labels
`MUS`, `product`, `stats`, `review`

## User Story
As a cuber, I want post-session summaries and weakness tracking, so that I can see what improved and what still needs work.

## Proposed Solution

### Overview
Plan review surfaces that turn training data into understandable outcomes and next recommendations.

### Implementation Flow
1. Define `SkillStats` and `TrainingSessionResult` outputs.
2. Define which signals appear in session review.
3. Define persistent weakness-summary surfaces beyond a single session.

### Technical Approach
Build on existing case-stat ideas where useful, but keep trainer review output as a first-class planning concern.

### Key Considerations
- Review should be understandable, not just data-dense.
- Weakness ranking and session recap must align with the weighting model.

## Acceptance Criteria

- [x] Session review specifies which metrics appear (e.g., avg time, best/worst cases, improvement trend, weakness ranking). ✅ Implemented with a summary stats row, hardest-cases grid, full results table, and weak-case recommendation card in `session-review.js`, plus persistent weakness ranking surfaces in `weakness-summary.js`
- [x] Persistent weakness summary defines what data persists across sessions and how it is computed. ✅ Computed from `SkillStats` stored in trainer persistence, with classification into Mastered/Learning/Weak/Untrained
- [x] Review outputs map to specific data fields produced by FR-003 weighting logic and FR-004 cross drill tracking. ✅ Uses `getAllStats()`, `loadSessions()`, and `getCatalog()` from trainerIntegration. Session review now shows full results table with distribution bars, vs-avg comparisons, and hardest cases grid
- [x] At least one "next recommended action" recommendation is defined based on review data. ✅ Recommendation card with weak-case chips and a `Drill Weak Cases` action that routes back into trainer setup with the current session goal preserved
